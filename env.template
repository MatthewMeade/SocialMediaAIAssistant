# Environment Variables Template
# Copy this file to .env.local and fill in your actual values

# ============================================
# CLIENT ENVIRONMENT VARIABLES (Vite)
# ============================================
# These variables are exposed to the browser and must be prefixed with VITE_
# They are bundled into the client code and visible in the browser

VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
VITE_SUPABASE_REDIRECT_URL=http://localhost:3000

# Langfuse Configuration (Client-side)
VITE_LANGFUSE_PUBLIC_KEY=your_langfuse_public_key
VITE_LANGFUSE_BASEURL=https://cloud.langfuse.com

# ============================================
# SERVER ENVIRONMENT VARIABLES
# ============================================
# These variables are only available on the server
# Never exposed to the client - safe for sensitive data

# Server Configuration
PORT=3001
NODE_ENV=development
CLIENT_URL=http://localhost:3000

# Supabase Configuration (Server-side)
SUPABASE_URL=your_supabase_project_url
SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Langfuse Configuration (Server-side)
LANGFUSE_PUBLIC_KEY=your_langfuse_public_key
LANGFUSE_SECRET_KEY=your_langfuse_secret_key
LANGFUSE_BASEURL=https://cloud.langfuse.com
